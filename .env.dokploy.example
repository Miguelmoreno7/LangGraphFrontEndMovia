APP_ENV=production
LOG_LEVEL=INFO

# Use managed Postgres (Supabase recommended)
DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME

# Leave unset to use internal Redis service from dokploy.compose.yml
# REDIS_URL=redis://redis:6379/0

QUEUE_NAME=runs:queue
API_PORT=8000
WORKER_MAX_RETRIES=2
WORKER_POLL_TIMEOUT_SECONDS=5

# Replace with your actual frontend domain(s), comma-separated if needed
CORS_ORIGINS=https://your-frontend-domain.com

# Optional, depending on your graph/tool usage
# OPENAI_API_KEY=...

